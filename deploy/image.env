# Default registry image for publish / VPS pull.
# Override with IMAGE=... when needed.
IMAGE_NAME="${IMAGE_NAME:-philluikgy/wasm-db-workbench}"
IMAGE_TAG="${IMAGE_TAG:-latest}"
IMAGE="${IMAGE:-${IMAGE_NAME}:${IMAGE_TAG}}"
